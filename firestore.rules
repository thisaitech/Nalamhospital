rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Clinic HR demo rules — open access for the custom email/password auth
    // stored in Firestore (not Firebase Auth). Tighten before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
